# zentinel Task Status

This file records implementation task state and handoffs. Documentation bootstrap has been completed, but no framework implementation task has started.

## Current State

| Field | Value |
| --- | --- |
| Active task | none |
| Next task | `tasks/030-doctest-conventions.md` |
| Sequential mode | enforced |
| Machine-readable state | `tasks/status.json` |
| TDD-first policy | required; mechanical chronology proof limited until pipeline artifact validation |
| Deterministic core policy | enforced |
| AI authority over correctness | forbidden |
| Validator scope | task-system consistency, not product semantic correctness |

## Bootstrap Record

| Field | Value |
| --- | --- |
| Date | 2026-05-19 |
| Scope | Repository documentation, governance contracts, sequential task files, doctest plans, AI-agent pipeline policies, and end-to-end backlog through release acceptance populated, including inserted tasks `061` through `070` and `085`, plus pre-bootstrap hardening tasks `071` through `105`. Task `105` closes architecture boundary enforcement before project bootstrap. |
| Implementation code changed | none |
| Verification run | Required file presence scan; governance file scan; ADR index scan; gap registry scan; task section completeness scan; Markdown/JSON task scope sync; follow-up reference sync; schema registry coverage; generated legacy-name and unfinished-marker scan; JSON parse checks; `python3 scripts/validate_task_system.py` |
| Dogfooding status | policy documented; no dogfood implementation exists yet |

## Completion Log

| Task | Date completed | Files changed | Tests added | Tests run | Deterministic behavior affected | Dogfooding implication | Known follow-ups |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 029 Phase 2 Semantic Dogfood | 2026-05-31 | test/dogfood_semantic_test.zig, test/fixtures/dogfood/semantic/sample.zig. | dogfood_semantic_test (every stable Phase 2 operator fires over the fixture; no invalid mutants via isValidCandidate; repeated runs deterministic in ids/order). | failing `zig build test` (3 dogfood tests FileNotFound before the fixture existed; one compile error on report-only display_id fixed) then passing all tests; `zig build`; `zig fmt --check src test build.zig`; `python3 scripts/validate_task_system.py`. | First Phase 2 semantic dogfood: drives the four stable Phase 2 recognizers (optional/error_path/integer_boundary/loop_boundary) directly through the shared collector over a semantic fixture (run_command not in scope and Phase 2 not yet command-wired), asserting all six operators fire, no invalid mutants, and deterministic repeated runs. No gap rows. FOLLOW-UP FLAGGED: Phase 2 recognizers are not invoked by run_command/list-mutants candidate generation (both run only the 4 Phase 1 recognizers), so enabling Phase 2 operators in config currently yields nothing from those commands. | First dogfood pass over Phase 2 semantic mutators; flags that they must be wired into run/list-mutants before run/CI tasks (043, 059). | `tasks/030-doctest-conventions.md`. |
| 028 Loop Boundary Mutator | 2026-05-31 | src/mutators/loop_boundary.zig, src/root.zig, test/mutators/loop_boundary_test.zig, test/fixtures/mutators/loop_boundary/{ops.zig,infinite.zig}, tests/coverage-gaps/mutators.v1.json. | loop_boundary_test (while boundary swap < -> <= compiles + for-range end +1/-1 may_fail in deterministic order; while-true skipped; while-cont boundary; non-literal range skipped; test-body exclusion). | failing `zig build test` (missing loop_boundary member; 224/224 prior) then passing all tests; `zig fmt --check src test build.zig`; `python3 scripts/validate_task_system.py`. | src/mutators/loop_boundary.zig (deterministic_core): swaps a while-condition comparison boundary (<,<=,>,>= via while node cond_expr; compiles) and increments/decrements a for-range end (for_range node_and_opt_node[1]) when it is a plain decimal literal (i128, may_fail). Infinite loops (while true), non-comparison conditions, open/non-integer range ends, and test bodies skipped; deterministic canonical order; equality (==,!=) not boundary-swapped. No ast_backend/mutant change. Closes loop_boundary -- ALL 12 minimum-product mutators now implemented. | Completes the minimum-product mutator set, unblocking Phase 2 semantic dogfood (029). | `tasks/029-phase2-semantic-dogfood.md`. |
| 027 Integer Literal Boundary Mutator | 2026-05-31 | src/mutators/integer_boundary.zig, src/root.zig, test/mutators/integer_boundary_test.zig, test/fixtures/mutators/integer_boundary/{ops.zig,protected.zig}, tests/coverage-gaps/mutators.v1.json. | integer_boundary_test (paired +1/-1 for branch + length-check literals in deterministic order, may_fail; protected declarations/enum/length/alignment yield none; zero->1/-1; hex skipped; assignment literals untouched; test-body exclusion). | failing `zig build test` (missing integer_boundary member; 218/218 prior) then passing all tests; `zig fmt --check src test build.zig`; `python3 scripts/validate_task_system.py`. | src/mutators/integer_boundary.zig (deterministic_core) emits paired +1/-1 candidates (i128 math, plain-decimal only, replacement strings via collector allocator) for number literals that are direct comparison operands (==,!=,<,<=,>,>=) -> branch and length-check boundaries; an allowlist by context naturally rejects protected literals (declarations, enum tags, array lengths, align) since they are not comparison operands; hex/binary/octal/underscore skipped; test bodies excluded; may_fail (overflow possible). Deterministic order via the collector's canonical sort. No ast_backend/mutant change. Closes integer_literal_boundary; 11/12 minimum-product mutators done. Also gitignored .claude/ (harness scheduling state, removed each iteration to satisfy the no-.claude validator rule). | Off-by-one boundary fixtures become future dogfood targets for config/report code; only loop_boundary (028) remains. | `tasks/028-loop-boundary-mutator.md`. |
| 026 Errdefer Mutator | 2026-05-31 | src/mutators/error_path.zig, test/mutators/errdefer_test.zig, test/fixtures/mutators/errdefer/{ops.zig,survivor.zig}, tests/coverage-gaps/mutators.v1.json. | errdefer_test (errdefer <body> -> errdefer {} may_fail; existing errdefer {} not mutated; block/expr body replaced wholesale; defer unchanged; test-body exclusion; survivor fixture candidate). | failing `zig build test` (no errdefer candidate, expected 1 found 0; existing tests passed) then passing all tests with catch/defer behavior unchanged; `zig fmt --check src test build.zig`; `python3 scripts/validate_task_system.py`. | Extended src/mutators/error_path.zig (deterministic_core) with stable errdefer_remove via a tag switch (catch + errdefer): collectErrdefer spans the whole `errdefer <body>` (errdefer keyword through cleanup body) and replaces it with the literal `errdefer {}`, skips an existing errdefer {}, excludes test bodies, may_fail (a captured resource used only in the cleanup can become unused). defer is a distinct node tag and is never recognized. Operator consts renamed for clarity; emitted strings unchanged. No ast_backend/mutant change. Closes errdefer_remove. Ten of twelve minimum-product mutators now implemented. | Extends Phase 2 semantic dogfood for cleanup-on-error; survivor fixture surfaces a never-exercised error cleanup. | `tasks/027-integer-literal-boundary-mutator.md`. |
| 025 Autonomous Backlog Audit | 2026-05-31 | tasks/QUEUE.md, tasks/queue.json, tasks/STATUS.md, tasks/status.json (state transition only; no backlog change). | None (audit task; no prerequisite task inserted, so no manufactured failure per the task contract). | `python3 scripts/validate_task_system.py` (passed, 106 tasks); JSON syntax validation of queue.json/status.json; comprehensive scan (0 missing guards/sections across all queued+active tasks, 0 missing queue.json fields); coverage cross-check of PROJECT_ACCEPTANCE_CRITERIA required mutators/commands/reports/dogfood/CI/AI/doctest/performance vs the queued backlog. | Audited the full backlog against docs/ROADMAP.md + docs/PROJECT_ACCEPTANCE_CRITERIA.md and found it complete and well-formed: all 12 required mutators have tasks (errdefer/integer_literal_boundary/loop_boundary deferred to 026/027/028); every roadmap phase 0-7 plus release acceptance (060) and final dogfood release gate (085) has concrete sequential tasks with no dead-end; every task file has a sequential guard + standard sections; every queue.json entry has deps/allowed/forbidden/phase/state; preview mutator optional_orelse_default not treated as required. No concrete missing or stale task, so no queue/task-file changes were made. | Confirms dogfooding expansion (029/059/064/085) is concrete and validated rather than prose; Stage 1 fixture dogfood already runs (022). | `tasks/026-errdefer-mutator.md`. |
| 024 Error Path Mutators | 2026-05-31 | src/mutators/error_path.zig, src/root.zig, test/mutators/error_path_test.zig, test/fixtures/mutators/error_path/{ops.zig,survivor.zig,killed.zig}, tests/coverage-gaps/mutators.v1.json. | error_path_test (catch handler->unreachable canonical order with 0 and -1; existing catch unreachable not mutated; catch |err| handler replaces only the handler `handle(err)`; survivor fixture candidate; killed fixture candidate with test body excluded). | failing `zig build test` (missing error_path mutator member; 207/207 prior) then passing all tests with Phase 1 + optional mutators still green; `zig fmt --check src test build.zig`; `python3 scripts/validate_task_system.py`. | src/mutators/error_path.zig (deterministic_core) implements error_catch_unreachable: walk `.@\"catch\"` nodes, take the handler (node_and_node[1]) over firstToken..lastToken, replace with unreachable preserving the exact handler span (|err| payload excluded, token-determined), skip existing catch unreachable + test bodies, may_fail compile expectation, equivalent-risk hints, shared collector. No ast_backend/mutant change. Re-exported via zentinel.mutators.error_path; run-pipeline enabling deferred (run_command.zig out of scope), reports need no schema change. Removed a stray untracked MISSING_COMMENT_INVENTORY.md scan artifact. Closes error_catch_unreachable. | Error-path mutators will later target zentinel's runner/config diagnostics where missing error tests are costly; survivor fixture shows the never-exercised catch branch. | `tasks/025-autonomous-backlog-audit.md`. |
| 023 Optional and Null Mutators | 2026-05-30 | src/mutators/optional.zig, src/root.zig, test/mutators/optional_test.zig, test/fixtures/mutators/optional/{ops.zig,survivor.zig}, tests/coverage-gaps/mutators.v1.json. | optional_test (orelse->unreachable in canonical order; all four null-check forms x==null/x!=null/null==x/null!=x; existing orelse unreachable not re-mutated; non-null equality deferred to comparison; survivor fixture candidate; test-body exclusion). | failing `zig build test` (missing optional mutator member; 201/201 prior) then passing all tests with Phase 1 fixtures still green; `zig fmt --check src test build.zig`; `python3 scripts/validate_task_system.py`. | src/mutators/optional.zig (deterministic_core) implements the first Phase 2 optional mutators: optional_orelse_unreachable replaces the orelse fallback expression (RHS via node_and_node[1], span over the fallback) with unreachable (skips existing orelse unreachable + test bodies, may_fail), and optional_null_check swaps == <-> != against the null literal (operand-order aware, compiles). Both use the shared collector; the comparison mutator continues to leave null equality here. No ast_backend/mutant change needed. Recognizers available via zentinel.mutators.optional; run-pipeline enabling deferred (run_command.zig out of scope) and reports need no schema change (operator names are free strings). Closes optional_orelse_unreachable, optional_null_check. | Optional mutators are strong config-parser dogfood candidates; the survivor fixture shows how a missing null-path test is surfaced. | `tasks/024-error-path-mutators.md`. |
| 022 Dogfood Fixture Run | 2026-05-30 | zentinel.dogfood.toml, scripts/dogfood.sh, test/dogfood_fixture_test.zig, test/fixtures/dogfood/sample/src/calc.zig. | dogfood_fixture_test (dogfood config parses + targets fixtures; two runs differing only in observation metadata produce equivalent normalized reports = orchestration determinism over 2 fixture candidates). | failing then passing `zig build test` (determinism test needs the dogfood config+fixture); `zig fmt --check src test build.zig`; `scripts/dogfood.sh` ran the binary -> completed, 2/2 killed, report archived at test/fixtures/dogfood/sample/zig-out/zentinel-dogfood/report.json; `python3 scripts/validate_task_system.py`. | First Stage 1 dogfood workflow: zentinel.dogfood.toml + scripts/dogfood.sh run zentinel over test/fixtures/dogfood/sample (arithmetic_add_sub + comparison_boundary, both killed by a same-file test), no network/AI, production modules unmutated, deterministic output path. Orchestration determinism is proven hermetically. Dogfooding finding: real zig-test evidence excerpts contain ASLR addresses, random seeds, and run-id workspace paths, so raw report bytes are not deterministic across binary runs — flagged for evidence normalization in runner/report (out of task 022 scope). No gap rows. | Activates Stage 1 dogfooding as the proof path for new mutators; surfaced the evidence-normalization gap needed for clean report diffs. | `tasks/023-optional-null-mutators.md`. |
| 021 Cache Key Design | 2026-05-30 | src/cache.zig, src/run_command.zig, src/cli.zig, src/root.zig, test/cache_key_test.zig, test/run_command_test.zig, test/snapshots/cache_metadata.json, tests/coverage-gaps/{failure_modes,invariants}.v1.json. | cache_key_test (key stability; every documented input changes the key; backend_version independent of backend name; sourceHash determinism/sensitivity; metadata snapshot) and run_command_test (--no-cache parses; disables result cache but keeps build-cache isolation and identical report). | failing `zig build test` (missing cache module; 192/192 prior) then a shadowing bug caught + fixed; passing all tests (cache-metadata snapshot re-asserts); `zig fmt --check src test build.zig`; `zig build`; binary writes cache.json (default metadata_only/1 key vs --no-cache disabled/0 keys, build-cache isolated both ways); `python3 scripts/validate_task_system.py`. | src/cache.zig (deterministic_core, pure key DESIGN, no storage): computeKey hashes the canonical PERFORMANCE_STRATEGY inputs (incl. backend_version separate from backend name, source content hash, Zig cache namespace) into a stable hex SHA-256; Metadata serializes result-cache keys distinctly from Zig build-cache reuse metadata. run_command computes per-mutant keys + run cache metadata on RunOutcome.cache (NOT the report, so no run snapshot moved); --no-cache disables the result cache without disabling build-cache isolation and never changes mutant correctness. Result reuse stays disabled (mode never read_write). Closes F-019, I-013. No worker-pool/ai/ast_backend changes. | Content-addressed keys are the prerequisite for fast, trusted dogfood runs; reuse stays disabled so no stale result is served yet. | `tasks/022-dogfood-fixture-run.md`. |
| 020 Test Selection Same File | 2026-05-30 | src/test_selection.zig, src/ast_backend.zig, src/config.zig, src/run_command.zig, src/root.zig, test/test_selection_test.zig, test/config_test.zig, test/fixtures/test_selection/**, test/snapshots/run_command_completed.json, tests/coverage-gaps/invariants.v1.json. | test_selection_test (same-file discovery + file/line/name ordering; preflight pass selects zig test <file>; preflight fail + no-tests fall back; generated-in-baseline needs no preflight; all strategy; run-integration preflight path proving the killed mutant stays in the report = I-012; selection-metadata snapshot) and config_test (impact_graph rejected, not downgraded). | failing `zig build test` (missing test_selection module; 183/183 prior) then passing all tests (selection-metadata + regenerated run snapshot re-assert); `zig fmt --check src test build.zig`; `zig build`; binary selects the same-file test on the killable fixture (generated==baseline so no preflight, fallback_used=false, mutant killed); `python3 scripts/validate_task_system.py`. | src/test_selection.zig (deterministic_core, pure) + ast_backend.testDecls implement same_file_then_package: discover same-file tests (sorted file/line/name), prefer `zig test <file>` when it is a baseline command or passes an unmutated preflight (phase=selection_preflight), else fall back to configured commands with fallback_used=true and the failed preflight recorded. run_command runs the preflight via the baseline executor before classifying and records real test_selection per mutant (this regenerated the task-016 run snapshot; that snapshot was added to task 020 allowed_files as a downstream consequence). config rejects impact_graph explicitly before task 051. Selection is AI-free and never hides a mutant. Closes I-012. No mutators/sandbox/cache/AI changes. | Same-file selection speeds future self-mutation while keeping a deterministic fallback and the unmutated preflight safety check; coverage is never hidden. | `tasks/021-cache-key-design.md`. |
| 018 Report Renderers | 2026-05-30 | src/report_text.zig, src/report_jsonl.zig, src/report_junit.zig, src/run_command.zig, src/cli.zig, src/root.zig, test/report_renderers_test.zig, test/run_command_test.zig, test/snapshots/report_{survivors.txt,basic.jsonl,diagnostic.xml}, tests/coverage-gaps/invariants.v1.json. | report_renderers_test (text/jsonl/junit snapshots; JUnit status mapping for all 7 statuses + baseline_failed + strict survivor-failing; JUnit command-evidence properties; JSON schema compat; derived-not-trusted summary; I-003 ordering; I-015 normalization) and run_command_test (jsonl/junit parse; format + verbosity do not change canonical data). | failing `zig build test` (missing report_text/jsonl/junit modules; 169/169 prior) then a real dangling-pointer segfault in test builders, fixed; then passing all tests (3 self-blessing renderer snapshots re-assert equality); `zig fmt --check src test build.zig`; `zig build`; binary renders text/json/jsonl/junit + --verbose/--quiet with canonical JSON unchanged; `python3 scripts/validate_task_system.py`. | report_text/jsonl/junit (deterministic_core) render the canonical report into survivor-focused text, streaming JSONL (header + one object per mutant), and JUnit XML (exact status mapping, derived counts, escaped command-evidence properties, time normalized to 0; survivors are failures only in strict --fail-on-survivors mode). run_command gained jsonl/junit + run-local --verbose/--quiet; cli always writes canonical JSON and selects only the stdout renderer. --verbose/--quiet are run-local so `--verbose check` -> passthrough (task 005 contract) is preserved. Closes I-003, I-015. No runner/mutators/cache/AI changes. | Dogfood review depends on clear survivor output; JUnit diagnostic mode never reports survivors as unit-test failures. | `tasks/020-test-selection-same-file.md`. |
| 017 List Mutants Command | 2026-05-30 | src/list_mutants_command.zig, src/cli.zig, src/root.zig, test/list_mutants_command_test.zig, test/snapshots/list_mutants_basic.{txt,json}. | list_mutants_command_test (deterministic generation + identical durable ids on re-run with no executor; operator filtering; parseArgs --operator/--format; text + JSON snapshots). | failing `zig build test` (missing list_mutants_command module; 164/164 prior) then passing all tests (2 self-blessing snapshots re-assert equality); `zig fmt --check src test build.zig`; `zig build`; binary `list-mutants` text/json/`--operator` listing with durable id matching the run report and no .zig-cache created (no execution); `python3 scripts/validate_task_system.py`. | src/list_mutants_command.zig (deterministic_core) generate() reuses the AST backend + 4 Phase 1 mutators through the shared collector to list the canonical candidate set (config-enabled operators + optional --operator), taking source bytes and no executor so listing never patches or runs tests; renderText (compact lines + count) and renderJson ({total, mutants:[shared mutant.Mutant fields]}). cli.zig + route() wire `list-mutants` with config load + glob discovery, no baseline/workspace. Only the stable AST backend; experimental backends untouched. No gap rows. No runner/sandbox/cache/AI changes. | Candidate listing lets self-mutation scope be reviewed before execution; shares durable m_... ids with run reports for consistent dogfood triage. | `tasks/018-report-renderers.md`. |
| 016 Minimal Run Command | 2026-05-30 | src/project_model.zig, src/run_command.zig, src/cli.zig, src/root.zig, test/project_model_test.zig, test/run_command_test.zig, test/fixtures/run_command/{sample,killable}/**, test/snapshots/run_command_{completed,baseline_failed,baseline_timeout}.json. | project_model_test (glob `*`/`**` matching, include/exclude eligibility, config-glob discovery over a fixture) and run_command_test (baseline-failure exit 3; killed; survived; --fail-on-survivors exit 1; --operator/--mutant filters; --output root restriction; run.jobs>1 rejected; parseArgs; 3 deterministic JSON snapshots). | failing `zig build test` (missing project_model/run_command modules; 147/147 prior) then passing all tests across all steps (snapshots self-bless then re-assert equality); `zig fmt --check src test build.zig`; `zig build` binary; binary end-to-end `zentinel --root test/fixtures/run_command/killable run` kills 1 mutant (exit 0), `--output ../escape.json` exit 2, `--operator logical_and_or` 0 mutants exit 0; `python3 scripts/validate_task_system.py`. | src/project_model.zig + src/run_command.zig (deterministic_core) discover files from config globs and orchestrate the Phase 1 flow over injected baseline/mutant providers into a deterministic report (baseline_failed/timeout->exit 3, killed/survived classification from the mutant runner, canonical display ids, summary derivation, line-level diffs, root-restricted --output, run.jobs>1 rejected); --fail-on-survivors changes only exit code. cli.zig wires real std.process.run executors with per-mutant isolated workspaces under .zig-cache/zentinel/workspaces (copy minus caches, patch, cwd=workspace, delete), native Io.Timeout, stable cwd label. No gap rows. No parallelism/cache/AI/experimental backends. | First task enabling meaningful fixture-only dogfooding: discovers files, runs a real baseline, and kills a real mutant in an isolated workspace producing a deterministic report. Env minimization beyond direct-argv/no-shell is a documented Phase 1 limitation. | `tasks/017-list-mutants-command.md`. |
| 015 Mutant Runner | 2026-05-30 | src/mutant_runner.zig, src/runner.zig, src/root.zig, test/mutant_runner_test.zig, test/fixtures/mutant_runner/target.zig, tests/coverage-gaps/failure_modes.v1.json, tests/coverage-gaps/invariants.v1.json. | mutant_runner_test (killed/survived/compile_error/compiler_crash/timeout/invalid classification, workspace-creation failure, fail-fast skipped commands, structured mutant evidence). | failing `zig build test` (missing mutant_runner module; 139/139 prior) then passing 147/147 across 43/43 steps; `zig build install` exe; `zig fmt --check src test`; `python3 scripts/validate_task_system.py`. | src/mutant_runner.zig (deterministic_core) combines mutant/sandbox/runner: injected workspace outcome + executor, fail-fast, classifier_source; failure_kind distinguishes compile_error from killed; compiler_crash distinct from compile_error/invalid/internal_error. Generalized runner.classifyCommand(phase). Closes F-010, F-014, F-015, F-016, F-017, F-032, I-010, I-021. No worker pool/cache. | Core execution behavior for Stage 1 fixture dogfooding; real spawning/workspaces injected and wired by the run command; no dogfood run yet. | `tasks/016-minimal-run-command.md`. |
| 014 Baseline Runner | 2026-05-30 | src/runner.zig, src/root.zig, test/runner_baseline_test.zig, test/fixtures/runner/baseline_command.json, tests/coverage-gaps/failure_modes.v1.json, tests/coverage-gaps/invariants.v1.json. | runner_baseline_test (passing/failing/timeout/compiler-crash classification, bounded excerpts, config-order argv reuse with shell=false, command-result snapshot). | failing `zig build test` (missing runner module; 132/132 prior) then snapshot bootstrap, then passing 139/139 across 41/41 steps; `zig build install` exe; `zig fmt --check src test`; `python3 scripts/validate_task_system.py`. | src/runner.zig (deterministic_core) parses commands via the shared parser, runs them through an injected Executor, and classifies baseline pass/fail/timeout/compiler_crash with bounded structured evidence; real process spawning is injected by the run command (side_effect). Closes F-013, F-042, I-001. No mutation behavior. | Baseline reliability gates dogfood; classification verified deterministically with a mock executor; no dogfood run yet. | `tasks/015-mutant-runner.md`. |
| 013 Patch Sandbox | 2026-05-30 | src/sandbox.zig, src/root.zig, test/sandbox_test.zig, test/fixtures/sandbox/target.zig, tests/coverage-gaps/failure_modes.v1.json, tests/coverage-gaps/invariants.v1.json. | sandbox_test (apply single mutation to copied workspace; original file unchanged; invalid spans produce sandbox: diagnostics with ZNTL_SANDBOX_PATCH_* codes; deterministic patched content). | failing `zig build test` (missing sandbox member; 128/128 prior) then passing 132/132 across 39/39 steps; `zig build install` exe; `zig fmt --check src test`; `python3 scripts/validate_task_system.py`. | src/sandbox.zig (deterministic_core) apply() validates span + original text then returns a patched copy; input buffer never modified (working tree unchanged). Per ARCHITECTURE.md the patch transformation is deterministic core; filesystem workspace creation (F-010 ZNTL_SANDBOX_CREATE_FAILED) is side_effect and re-deferred to task 015 (mutant-runner). Covers F-011, F-012, I-007, I-008. No execution. | Safe deterministic patching is required before zentinel mutates its own files; filesystem isolation lands with the runner; no dogfood run yet. | `tasks/014-baseline-runner.md`. |
| 012 Logical and Boolean Mutators | 2026-05-30 | src/mutators/logical.zig, src/mutators/boolean.zig, src/root.zig, test/mutators/logical_boolean_test.zig, test/fixtures/mutators/logical_boolean/ops.zig, tests/coverage-gaps/mutators.v1.json. | logical_boolean_test (and<->or with exact keyword span; true<->false; stable mixed-mutator ordering; comment/string booleans not mutated). | failing `zig build test` (missing logical/boolean members; 124/124 prior) then passing 128/128 across 37/37 steps; `zig build install` exe; `zig fmt --check src test`; `python3 scripts/validate_task_system.py`. | src/mutators/logical.zig (.bool_and/.bool_or keyword swaps) and src/mutators/boolean.zig (.identifier true/false swaps, excludes enum literals/comments/strings) via the shared collector; compiles expected_compile; equivalent_risks from spec. Completes the Phase 1 stable expression-mutator set. Closes logical_and_or, boolean_literal. No patching or execution. | Will surface missing branch tests in config/report code once execution exists; no dogfood run yet. | `tasks/013-patch-sandbox.md`. |
| 011 Comparison Mutators | 2026-05-30 | src/mutators/comparison.zig, src/root.zig, test/mutators/comparison_test.zig, test/fixtures/mutators/comparison/ops.zig, tests/coverage-gaps/mutators.v1.json. | comparison_test (== != < <= > >= swaps in canonical order with compiles + equivalent-risk hints; null comparisons skipped; comment/string operators not mutated). | failing `zig build test` (missing comparison member; 121/121 prior) then passing 124/124 across 35/35 steps; `zig build install` exe; `zig fmt --check src test`; `python3 scripts/validate_task_system.py`. | src/mutators/comparison.zig (table-driven) emits equality_swap and comparison_boundary swaps via the shared collector with exact token spans; `== null`/`!= null` reserved for optional_null_check; equivalent_risks carried from MUTATOR_SPEC; expected_compile compiles. Closes equality_swap, comparison_boundary. No patching or execution. | Expected high-value dogfood operators for config/selection code once execution exists; no dogfood run yet. | `tasks/012-logical-boolean-mutators.md`. |
| 010 Arithmetic Mutators | 2026-05-30 | src/mutators/arithmetic.zig, src/root.zig, test/mutators/arithmetic_test.zig, test/fixtures/mutators/arithmetic/ops.zig, tests/coverage-gaps/mutators.v1.json. | arithmetic_test (+/-/*// swap candidates in canonical order with may_fail + ids; unary minus not mutated; test-body operators excluded). | failing `zig build test` (missing mutators module + @embedFile package-path; 118/118 prior) then passing 121/121 across 33/33 steps; `zig build install` exe; `zig fmt --check src test`; `python3 scripts/validate_task_system.py`. | src/mutators/arithmetic.zig recognizes .add/.sub/.mul/.div (not wrapping/unary), emits +<->- and *</> swaps via the shared collector with exact operator token spans, test-body exclusion, and may_fail expected_compile (no type info at AST layer). Closes arithmetic_add_sub, arithmetic_mul_div. No patching or execution. | First candidate-emitting recognizer; a basic dogfood signal but not run on core until runner/reports exist; no dogfood run yet. | `tasks/011-comparison-mutators.md`. |
| 019 Same-File Test Exclusion | 2026-05-30 | src/ast_backend.zig, test/same_file_test_exclusion_test.zig, test/fixtures/same_file_tests/mixed.zig, tests/coverage-gaps/invariants.v1.json. | same_file_test_exclusion_test (test-decl ranges classify production vs test-body offsets; candidate filter keeps production, drops test-body). | failing `zig build test` (missing testDeclRanges; then wrong nodeToSpan ranges during diagnosis; 116/116 prior) then passing 118/118 across 31/31 steps; `zig build install` exe; `zig fmt --check src test`; `python3 scripts/validate_task_system.py`. | ast_backend.testDeclRanges/inTestBody/excludeTestBodyCandidates exclude `test` declaration bodies via deterministic token brace-matching (std.zig.Ast lastToken under-reports test_decl spans in 0.16); production code in the same file stays mutable; no config option, no operators enabled. Closes I-009. | Protects future dogfood runs from mutating zentinel's own tests; no dogfood run yet. | `tasks/010-arithmetic-mutators.md`. |
| 009 AST Candidate Ordering | 2026-05-30 | src/mutant.zig, src/ast_backend.zig, test/ast_candidate_ordering_test.zig, tests/coverage-gaps/invariants.v1.json. | ast_candidate_ordering_test (canonical ordering by file/span/operator/replacement/backend with ids assigned, exact-identity dedupe, order stability across insertion orders). | failing `zig build test` (missing Collector; 113/113 prior) then passing 116/116 across 29/29 steps; `zig build install` exe; `zig fmt --check src test`; `python3 scripts/validate_task_system.py`. | mutant.assignId + sortAndDedupe and ast_backend.Collector add deterministic candidate collection on the shared model; no operators or recognizers enabled. Closes I-002. | Stable candidate ordering keeps dogfood diffs reviewable and worker scheduling from reordering reports; no dogfood run yet. | `tasks/019-same-file-test-exclusion.md`. |
| 008 AST Parser Spike | 2026-05-30 | src/source_map.zig, src/ast_backend.zig, src/root.zig, test/ast_parser_test.zig, test/fixtures/ast_parser/valid.zig, test/fixtures/ast_parser/invalid.zig.txt, tests/coverage-gaps/failure_modes.v1.json. | ast_parser_test (byte<->line/col round-trip + EOF, out-of-range guardrails, valid parse, parse diagnostics with file+location, deterministic node-index traversal). | failing `zig build test` (missing source_map/ast_backend; 108/108 prior) then passing 113/113 across 27/27 steps; `zig build install` exe; `zig fmt --check src test` (invalid fixture stored as .zig.txt so fmt skips it); `python3 scripts/validate_task_system.py`. | Pure source mapping + std.zig.Ast 0.16.0 adapter (parse/diagnostics/deterministic traversal) wrapped in one module; verified API surface recorded in completion evidence. Closes F-007, F-008. No mutators enabled. | Source mapping correctness underpins all dogfood reports; mapping tested before mutators depend on it; no dogfood run yet. | `tasks/009-ast-candidate-ordering.md`. |
| 007 Mutant Model | 2026-05-29 | src/mutant.zig, src/report.zig, src/root.zig, test/mutant_model_test.zig, tests/coverage-gaps/failure_modes.v1.json, tests/coverage-gaps/invariants.v1.json. | mutant_model_test (m_ derivation + independently-computed pinned vector, ^m_[A-Za-z0-9]+$ pattern, backend_version identity participation, non-canonical-field independence, canonical sort, span validation, invalid-candidate contract, report shared-type consumption). | failing `zig build test` (missing mutant module; 100/100 prior) then passing 108/108 across 25/25 steps; `zig build install` exe; `zig fmt --check src test`; `python3 scripts/validate_task_system.py`. | Shared mutant model + durable m_... identity (sha256 + lowercase unpadded Crockford base32, 26-char truncation) + canonical ordering + structural invalid-candidate contract in deterministic_core; report.zig now consumes the shared Span/Backend/stability/compile-expectation types (one identity source). Closes F-009, I-011. No candidate generation, patching, or execution. | Stable mutant identity is central to dogfood triage; ids must stay byte-identical and migrate explicitly; no dogfood run yet. | `tasks/008-ast-parser-spike.md`. |
| 006 Report Schema | 2026-05-29 | src/report.zig, src/root.zig, test/report_schema_test.zig, test/snapshots/report_minimal.json, tests/coverage-gaps/schemas.v1.json, tests/coverage-gaps/failure_modes.v1.json. | report_schema_test (minimal serialization snapshot, canonical sorting + display ids, summary derivation, semantic validator across run/baseline cross-field rules, command evidence, skip_reason, invalid prefix, repeated-run normalization, and structural shape guards). | failing `zig build test` (missing report module; 79/79 prior) then snapshot bootstrap, then passing 100/100 across 23/23 steps; `zig build install` exe; `zig fmt --check src test`; `python3 scripts/validate_task_system.py`. | Typed report model + deterministic std.json serialization + semantic validator (second oracle for derived invariants) + repeated-run normalization in deterministic_core; schema file already matched and was unchanged. Closes zentinel.report.v1, F-018, F-035. No execution or mutant generation. | Report JSON is a high-value dogfood target (users, CI, editors, AI flows); not yet command-wired (task 016+); no dogfood run yet. | `tasks/007-mutant-model.md`. |
| 005 Version Policy and Check Command | 2026-05-29 | src/zig_version.zig, src/command.zig, src/check_command.zig, src/config.zig, src/cli.zig, src/root.zig, test/zig_version_test.zig, test/check_command_test.zig, test/snapshots/zig_unsupported.txt, test/fixtures/check/*.toml, tests/coverage-gaps/failure_modes.v1.json, tests/coverage-gaps/invariants.v1.json. | zig_version_test (version classification, tokens, unsupported snapshot, version status line) and check_command_test (shared command parser, check success/failures, global-option routing). | failing `zig build test` (missing module/route compile errors; 49/49 prior) then passing 79/79 across 21/21 steps; `zig build install` exe; `zig fmt --check src test`; built binary verified for version/check (valid, missing, bad-command, unknown, empty temp dir); `python3 scripts/validate_task_system.py`. | Pinned Zig 0.16.0 policy module + shell-free shared command parser + pure `check` orchestration in deterministic_core; thin cli.zig adapter runs `zig version` and reads config. Closes F-001/F-002/F-039/F-041 and I-005/I-006. Durable evidence: pinned 0.16.0, local `zig version` 0.16.0, match. No mutation behavior. | `zentinel check` is the first environment gate dogfood relies on; version validation guards every run; no dogfood run yet. | `tasks/006-report-schema.md`. |
| 004 Fixture System | 2026-05-29 | build.zig, test/support/fixture.zig, test/fixture_system_test.zig, test/fixtures/projects/arithmetic_kill/{calc.zig,fixture.toml}, test/fixtures/projects/no_eligible_sources/{fixture.toml,README.md}, tests/coverage-gaps/failure_modes.v1.json. | test/fixture_system_test.zig (deterministic enumeration, metadata load + validation, path normalization) and the arithmetic_kill fixture compile-test. | failing `zig build test` (missing loader/fixtures compile error; 44/44 prior tests still passed) then passing 49/49 tests across 17/17 steps; `zig build install` exe; `zig fmt --check build.zig src test`; `python3 scripts/validate_task_system.py`. | Deterministic fixture enumeration + in-tree TOML metadata loader (reusing config_toml via the root.zig hub and the harness normalizer); build.zig compiles the minimal fixture through a test command. Closes F-006. No mutation behavior. | Fixture projects under test/fixtures/projects are Stage 1 dogfood inputs; the no_eligible_sources fixture pins F-006; no dogfood run yet. | `tasks/005-version-policy.md`. |
| 003 Test Harness | 2026-05-29 | build.zig, test/support/harness.zig, test/harness_test.zig, test/fixtures/harness/test_discovery/nested_probe_test.zig. | test/harness_test.zig and the nested discovery probe. | failing `zig build test` (harness const-assignment compile error) then passing 44/44 across 6 recursively discovered files; `zig build` exe; `zig fmt --check`; `python3 scripts/validate_task_system.py`. | Deterministic snapshot/path/duration normalization helpers and isolated temp dirs; build.zig now discovers test/**/*_test.zig recursively so nested future tests need no build.zig edit. No mutation behavior. | Normalizes volatile output so future dogfood/report snapshots are stable; no dogfood run yet. | `tasks/004-fixture-system.md`. |
| 002 Config Parser | 2026-05-29 | src/config.zig, src/config_toml.zig, src/cli.zig, src/main.zig, src/root.zig, test/config_test.zig, test/cli_test.zig, tests/coverage-gaps/failure_modes.v1.json. | test/config_test.zig (TOML subset parser, validation, mutator expansion, init output). | failing `zig build test` (path normalization, 36/37) then passing 39/39 across 4 files; `zig build` exe; binary verified for config-aware init; `zig fmt --check`; `python3 scripts/validate_task_system.py`. | In-tree TOML subset parser and deterministic config validation in deterministic_core; config-aware init wired via root.zig dispatch; allowed_files corrected to include src/root.zig as the deterministic-core module-export hub. Closes F-003/F-004/F-005/F-040. No mutation behavior. | Config validation is a deterministic, branch-heavy early dogfood target; no dogfood run yet. | `tasks/003-test-harness.md`. |
| 001 CLI Shell | 2026-05-29 | src/root.zig, src/cli.zig, src/main.zig, test/cli_test.zig, test/snapshots/cli_help.txt, test/snapshots/init_config.toml. | test/cli_test.zig and CLI help/config snapshots. | failing `zig build test` (root.zig reverted) showed cli_test has no member Outcome; passing `zig build test` ran 15/15 tests across 3 files; `zig build` compiled the exe; binary verified for --help, version, run/unknown exit 2, and init create/refuse/force; `zig fmt --check`; `python3 scripts/validate_task_system.py`. | Pure CLI dispatch in deterministic_core (root.zig) with thin presentation adapter (cli.zig) and Zig 0.16 std.process.Init entry (main.zig); version is policy-only. No mutation behavior. | CLI help and config text are stable surfaces for future dogfood review; no dogfood run yet. | `tasks/002-config-parser.md`. |
| 000 Project Bootstrap | 2026-05-29 | build.zig, build.zig.zon, src/root.zig, src/main.zig, test/bootstrap_test.zig, test/bootstrap_discovery_test.zig. | test/bootstrap_test.zig, test/bootstrap_discovery_test.zig. | failing `zig build test` captured `error: no build.zig file found` before the scaffold; passing `zig build test` ran 5/5 steps and 3/3 tests across 2 auto-discovered files; `zig fmt --check build.zig src test`; `python3 scripts/validate_task_system.py`. | First runtime scaffold: root module exposes deterministic project_name and version constants; src files declare architecture layers. No mutation behavior yet. | Creates the codebase shape future dogfood tasks will mutate; no dogfood run yet. | `tasks/001-cli-shell.md`. |
| 105 Architecture Boundary Enforcement | 2026-05-23 | Task metadata, task 000 dependency guard, ADR-0008, architecture/internal API/invariant/glossary/discipline/agent-protocol docs, Codex role profiles, invariant gap row I-022, and validator architecture-boundary guardrails. | Structural validator guardrails requiring ADR-0008, deterministic pipeline boundary wording, layer registry wording, role review duties, I-022 coverage, task 105/000 dependency ordering, and future `src/**/*.zig` layer/import-boundary checks. | Post-activation `python3 scripts/validate_task_system.py`; pre-fix `python3 scripts/validate_task_system.py` failed on task 105 guardrails for missing ADR and boundary wording as expected; `python3 scripts/validate_task_system.py` while task `105` was active after fixes; `python3 -m py_compile scripts/validate_task_system.py`; `jq empty tasks/status.json tasks/queue.json tests/coverage-gaps/invariants.v1.json`; `git diff --check`; final complete-state validator run. | No runtime behavior exists yet; future source files must declare architecture layers and deterministic core imports of adapters are validator-rejected once `src/` exists. | Bootstrap starts after architecture boundaries are ADR-backed and validator-guarded. | `tasks/000-project-bootstrap.md`. |
| 104 Output Bound Wording Cleanup | 2026-05-21 | Task metadata, task 000 dependency guard, task 096 historical wording, schema gap row `zentinel.ai.context.v1`, status evidence wording, and validator guardrails. | Structural validator guardrail rejecting stale character-unit output-bound wording in AI-context cleanup targets. | Post-activation `python3 scripts/validate_task_system.py` initially failed on task 000 missing dependency 104; activation validator passed after updating the task 000 guard; pre-fix `python3 scripts/validate_task_system.py` failed on task 104 guardrails for stale output-bound wording and hardening-range status wording; `python3 scripts/validate_task_system.py` while task `104` was active after fixes; `python3 -m py_compile scripts/validate_task_system.py`; `jq empty tasks/status.json tasks/queue.json tests/coverage-gaps/schemas.v1.json`; `git diff --check`; stale phrase scan; final complete-state validator run. | No runtime behavior exists yet; future AI-context implementation guidance now consistently uses 4096 UTF-8 bytes. | Bootstrap starts after stale output-bound wording cleanup is validator-backed. | `tasks/000-project-bootstrap.md`. |
| 103 Contract Ambiguity Cleanup | 2026-05-21 | Task metadata, task 000 dependency guard, task 002 config parser boundary, task 053 AI-context output-bound requirements, task-plan activation workflow, config/AI/schema docs, and validator guardrails. | Structural validator guardrails for command-parser ownership, UTF-8 byte output bounds, task-plan activation branch wording, task 103/000 dependency ordering, and pre-063 structured chronology labels for future behavior-changing tasks. | Post-activation `python3 scripts/validate_task_system.py` failed on task 000 missing dependency 103 as expected after inserting task 103; pre-fix `python3 scripts/validate_task_system.py` failed on task 103 guardrails for status wording, task-plan wording, command-parser ownership, output-bound wording, and schema-bound caveat; `python3 scripts/validate_task_system.py` while task `103` was active after fixes; `python3 -m py_compile scripts/validate_task_system.py`; `jq empty tasks/status.json tasks/queue.json`; `git diff --check`; final complete-state validator run. | No runtime behavior exists yet; future implementation now starts with unambiguous command-parser ownership, canonical output excerpt bounds, and stricter pre-063 chronology evidence checks. | Bootstrap starts after final contract ambiguity cleanup is validator-backed. | `tasks/000-project-bootstrap.md`. |
| 102 Agent Workflow Cleanup | 2026-05-21 | Task metadata, task 000 dependency guard, task-plan activation workflow wording, task 101 validation-evidence notes, and validator guardrails. | Structural validator guardrails for task-plan activation wording deduplication, task 101 validation-evidence precision, task 102/000 dependency ordering, and pre-bootstrap hardening status wording. | Post-activation `python3 scripts/validate_task_system.py` failed on stale hard-coded task 101 final-prerequisite guardrails as expected after inserting task 102; pre-fix `python3 scripts/validate_task_system.py` failed on task 102 guardrails as expected; `python3 scripts/validate_task_system.py` while task `102` was active after fixes; `python3 -m py_compile scripts/validate_task_system.py`; `jq empty tasks/status.json tasks/queue.json`; `git diff --check`; final complete-state validator run. | No runtime behavior exists yet; future implementation now starts with deduplicated task-plan startup wording and explicit task 101 active/complete validation evidence. | Bootstrap starts after remaining agent-workflow cleanup is validator-backed. | `tasks/000-project-bootstrap.md`. |
| 101 Version Command and Evidence Closure | 2026-05-21 | Task metadata, task 000 dependency guard, task 005 version-policy requirements, CLI and failure-mode contracts, pre-063 TDD evidence docs, pipeline artifact activation wording, and validator guardrails. | Structural validator guardrails for task 005 version-command ownership, check-command fatal Zig diagnostics, pre-063 structured chronology evidence labels, task 101/000 dependency ordering, and post-041 activation wording deduplication. | Post-activation `python3 scripts/validate_task_system.py` failed on stale hard-coded task 000-after-100 guardrails as expected after inserting task 101; pre-fix `python3 scripts/validate_task_system.py` failed on task 101 guardrails as expected; `python3 scripts/validate_task_system.py` while task `101` was active after fixes; `python3 -m py_compile scripts/validate_task_system.py`; `jq empty tasks/status.json tasks/queue.json`; `git diff --check`; final complete-state validator run. | No runtime behavior exists yet; future implementation now starts with validator-backed ownership for `zentinel version` Zig discovery and pre-063 chronology evidence labels. | Bootstrap starts after version-command discovery ownership and early evidence conventions are unambiguous. | `tasks/000-project-bootstrap.md`. |
| 100 Clean Handoff Lifecycle Closure | 2026-05-21 | Task metadata, clean handoff lifecycle docs, task 000 dependency guard, task 041 gap-row reminder, protocol startup wording, and validator guardrails. | Structural validator guardrails for clean-worktree baseline nullability, clean handoff lifecycle docs, task 041 pipeline schema gap-row reminders, protocol duplicate wording, and task 000 dependency on task 100. | Post-activation `python3 scripts/validate_task_system.py` initially failed on stale hard-coded task 099 guardrails as expected; pre-fix `python3 scripts/validate_task_system.py` failed on task 100 guardrails as expected; `python3 scripts/validate_task_system.py` while task `100` was active after fixes; `python3 -m py_compile scripts/validate_task_system.py`; `jq empty tasks/status.json tasks/queue.json`; `git diff --check`; initial complete-state validator failed on active-state handoff wording and the guardrail was corrected; final complete-state validator run. | No runtime behavior exists yet; future implementation now starts from committed clean worktrees with `clean_handoff_baseline` cleared and stale baselines rejected. | Bootstrap starts after committed-handoff baseline lifecycle rules are validator-backed. | `tasks/000-project-bootstrap.md`. |
| 099 Handoff Baseline and Contract Drift Closure | 2026-05-21 | Task metadata, validator baseline hashing, status schema, task-status clean handoff metadata, agent workflow docs, AI/report/CLI/mutator/backend/doctest contracts, affected future task scopes, and schema gap row `zentinel.tasks.status.v1`. | Structural validator guardrails for clean handoff baseline metadata, baseline-aware changed-file checks, post-041 startup ordering, AI prompt skip evidence, invalid classifier evidence, version-policy task boundaries, semantic mutator filtering, experimental backend diagnostics, and mutation-aware doctest runner evidence. | Pre-fix `python3 scripts/validate_task_system.py` failed on task 099 guardrails as expected; `python3 scripts/validate_task_system.py` while task `099` was active after fixes; `python3 -m py_compile scripts/validate_task_system.py`; `jq empty tasks/status.json tasks/queue.json tasks/schema/status.v1.schema.json`; `git diff --check`; final complete-state validator run. | No runtime behavior exists yet; future implementation now starts from validator-readable dirty-file baselines and closed downstream contracts for reports, CLI, mutators, experimental backends, and doctest AI evidence. | Bootstrap starts after validator-readable handoff baselines and downstream contract drifts are closed. | `tasks/000-project-bootstrap.md`. |
| 098 Agent Implementation Blocker Closure | 2026-05-21 | Task metadata, validator guardrails, clean handoff boundary docs, I-019 chronology ownership, pipeline schema fixture and validator scopes, AI context schema/docs, sandbox phase labels, pre-041 handoff paths, Contract Editor ordering, and final dogfood archive paths. | Structural validator guardrails for clean handoff boundaries, I-019 deferral, task 041/063 pipeline schema scope, post-041 activation order, AI context result-level `skip_reason`, `selection_preflight`, pre-041 handoff location, Contract Editor/Test Author ordering, and dogfood archive paths. | Post-activation `python3 scripts/validate_task_system.py`; pre-fix `python3 scripts/validate_task_system.py` failed on task 098 guardrails as expected; post-fix `python3 scripts/validate_task_system.py` while task `098` was active; `python3 -m py_compile scripts/validate_task_system.py`; `jq empty` on edited JSON/schema/gap files; `git diff --check`; final active-scope and complete-state validator runs. | No runtime behavior exists yet; future implementation now starts from explicit handoff-boundary, chronology, pipeline-artifact, AI-context, sandbox, contract-edit, and dogfood archive contracts. | Bootstrap starts after the remaining autonomous implementation blockers are closed. | `tasks/000-project-bootstrap.md`. |
| 097 Autonomous Agent Contract Closure | 2026-05-21 | Task metadata, validator guardrails, report schema and report/test-selection/doctest docs, pipeline artifact/schema ownership docs and future task scopes, blocked-task lifecycle docs, TDD/invariant wording, Contract Editor role contracts, experimental backend CLI ownership, and schema gap rows for pipeline artifact schemas. | Structural validator guardrails for report preflight evidence, result-level skipped reasons, pipeline schema ownership, blocker JSON shape, TDD chronology proof wording, Contract Editor routing, experimental backend CLI opt-in ownership, doctest `run.error`, active-task resume, and Markdown queue row order. | Pre-fix `python3 scripts/validate_task_system.py` failed on task 097 guardrails as expected; post-fix `python3 scripts/validate_task_system.py` while task `097` was active; `python3 -m py_compile scripts/validate_task_system.py`; `jq empty` on edited JSON/schema/gap files; `git diff --check`; final `python3 scripts/validate_task_system.py` after completion. | No runtime behavior exists yet; future implementation now starts from canonical report preflight evidence, baseline pipeline schema ownership at task 041, stricter blocker and TDD evidence wording, and explicit contract/backend ownership. | Bootstrap starts after the remaining autonomous-agent implementation blockers are closed. | `tasks/000-project-bootstrap.md`. |
| 096 Audit Finding Contract Closure | 2026-05-21 | Task metadata, validator guardrails, test-selection/sandbox/architecture/lifecycle/routing docs, AI context schema and prompt docs, report/CLI/config contracts, affected future task scopes, and schema gap row `zentinel.ai.context.v1`. | Structural validator guardrails for same-file preflight, post-041 pipeline artifact exception scope, Planner role coverage, AI prompt `failure_kind`, AI context excerpt bounds, follow-up timing, queued terminology, high-risk routing, report semantic validation, CLI output bounds, and mode-matrix compatibility. | Pre-fix `python3 scripts/validate_task_system.py` failed on task 096 guardrails as expected; post-fix `python3 scripts/validate_task_system.py` while task `096` was active; `python3 -m py_compile scripts/validate_task_system.py`; `jq empty` on edited JSON files; `git diff --check`; final `python3 scripts/validate_task_system.py` after completion. | No runtime behavior exists yet; future implementation now starts from validator-backed audit-finding closures and stricter contract boundaries. | Bootstrap starts after the remaining autonomous-agent blockers are closed. | `tasks/000-project-bootstrap.md`. |
| 095 Autonomous Agent Contract Repair | 2026-05-21 | Task metadata, validator guardrails, task queue schema, Codex agent contracts, lifecycle/pipeline/gap docs, AI context and report schemas, affected future task scopes, and invariant gap row I-017. | Structural validator guardrails for active-only task-control states, stale lifecycle wording, inactive dirty-file scope evidence, row-scoped gap-registry diffs, AI context command `failure_kind`, baseline compiler-crash report schema, pre-041 artifact cutovers, `impact_graph` task scopes, and I-019 proof scope. | Pre-fix `python3 scripts/validate_task_system.py` failed on task 095 guardrails as expected; post-fix `python3 scripts/validate_task_system.py` while task `095` was active; `python3 -m py_compile scripts/validate_task_system.py`; JSON syntax checks for edited task/schema/gap JSON files; stale lifecycle phrase scan; `git diff --check`; final `python3 scripts/validate_task_system.py` after completion. | No runtime behavior exists yet; future implementation now starts from active-only task-control states, final dirty-file scope evidence, aligned AI/report command schemas, and validator-backed future task scopes. | Bootstrap starts after the latest autonomous-agent contract blockers are closed. | `tasks/000-project-bootstrap.md`. |
| 094 Agent Readiness Validator Closure | 2026-05-21 | Task metadata, validator guardrails, status/report schemas, lifecycle/routing/gap docs, config/mutator/test-selection/report/sandbox/doctest/AI/internal contracts, and queued task requirements. | Structural validator guardrails for completed-task scope evidence, blocked-task prerequisite recovery, lifecycle/routing contracts, safety-mode report ownership, command `failure_kind` classification, staged mutator/test-selection options, sandbox evidence bounds, backend-version visibility, doctest AI stubs, and plain `zig` doctest expectations. | Pre-fix `python3 scripts/validate_task_system.py` failed on the new guardrails as expected; post-fix `python3 scripts/validate_task_system.py` while task `094` was active; `python3 -m py_compile scripts/validate_task_system.py`; JSON syntax checks for edited task/schema JSON files; `git diff --check`; final `python3 scripts/validate_task_system.py` after completion. | No runtime behavior exists yet; future implementation now starts from validator-backed completion scope evidence, blocked-prerequisite recovery, report evidence classification, staged option ownership, doctest AI stub mappings, and public/internal schema boundaries. | Bootstrap starts after the final four-lane readiness findings are machine-checkable. | `tasks/000-project-bootstrap.md`. |
| 093 Agent Enforcement Closure | 2026-05-21 | Task metadata, validator guardrails, status schema, task completion workflow docs, lifecycle/protocol/TDD docs, CLI/config/doctest/report/backend/property/gap contracts, and queued task requirements. | Structural validator guardrails for active-before-complete scope wording, blocked-task metadata, completion evidence artifacts, safety-mode staging, doctest exit/source-ref rules, classifier authority, backend version identity, gap registry closure, property evidence cutover, and affected future task specs. | Pre-fix `python3 scripts/validate_task_system.py` failed on the new guardrails as expected; post-fix `python3 scripts/validate_task_system.py` while task `093` was active; `python3 -m py_compile scripts/validate_task_system.py`; JSON syntax checks for edited task JSON/schema files; `git diff --check`. | No runtime behavior exists yet; future implementation now starts from stricter task completion, blocker recovery, evidence, doctest, backend, cache, and property-test contracts. | Bootstrap starts after active-scope validation ordering, typed blocker records, and deterministic classifier/backend-version rules are validator-backed. | `tasks/000-project-bootstrap.md`. |
| 092 Agent Readiness Follow-up Hardening | 2026-05-20 | Task metadata, validator guardrails, status schema, doctest task contract, handoff and pipeline docs, Codex agent role summary, backend docs, backend experiment tasks, and report format docs. | Structural validator guardrails for doctest fixture evidence, handoff role coverage, canonical reviewer wording, experimental backend report-schema alignment, pinned Zig backend wording, task-state schema shape, completed-task sync, extra Markdown queue rows, blocked-state details, and active-task changed-file scope. | Pre-fix `python3 scripts/validate_task_system.py` failed on the new guardrails as expected; post-fix `python3 scripts/validate_task_system.py`; `python3 -m py_compile scripts/validate_task_system.py`; `python3 -m json.tool` on edited JSON files; `git diff --check`. | No runtime behavior exists yet; future implementation starts from stricter task-state validation and closed report-v1 backend contracts. | Bootstrap starts with deterministic handoff names, cumulative test-evidence semantics, and validator-backed doctest fixture requirements before dogfoodable product code exists. | `tasks/000-project-bootstrap.md`. |
| 091 Analysis Risk Cleanup | 2026-05-20 | Task metadata, validator guardrails, ADR supersession wording, status handoff wording, and task 000 dependency/guard. | Structural validator guardrails for ADR-0001 supersession clarity and task-status handoff range clarity. | Pre-fix `python3 scripts/validate_task_system.py` failed on the new guardrails as expected; post-fix `python3 scripts/validate_task_system.py`; `python3 -m py_compile scripts/validate_task_system.py`; `jq empty` on task JSON files; `git diff --check`. | No runtime behavior exists yet; future implementation now starts from unambiguous Zig policy history and complete pre-bootstrap task-status wording. | Future implementation agents see the current pinned Zig policy and the full pre-bootstrap hardening range before starting bootstrap. | `tasks/000-project-bootstrap.md`. |
| 090 Contract Traceability and Scope Hardening | 2026-05-20 | Task metadata, validator guardrails, Zig policy docs, ADR records, diagnostic docs, failure-mode gap rows, TDD/agent protocol docs, preview mutator scope docs, and invariant gap row. | Structural validator guardrails for public diagnostic traceability, Zig `0.16.0` pinning, task-specific failing evidence, and preview mutator scope. | Pre-fix `python3 scripts/validate_task_system.py` failed on the new guardrails as expected; post-fix `python3 scripts/validate_task_system.py`; `python3 -m py_compile scripts/validate_task_system.py`; `jq empty` on task/gap JSON files; public error-code traceability scan; `zig version`. | No runtime behavior exists yet; future implementation now uses complete diagnostic traces, a fixed Zig `0.16.0` policy, and task-specific evidence beyond validator success. | Future dogfood and release work must use the pinned Zig contract and stable-mutator-only minimum scope. | `tasks/000-project-bootstrap.md`. |
| 089 Zig Version Verification Blocker Policy | 2026-05-20 | Task metadata, task 000 guard, Zig version policy, task 005 requirements, and validator guardrails. | Structural validator guardrails requiring task 005 to block instead of guessing when official latest-stable Zig verification is unavailable; superseded by task 090's Zig `0.16.0` pin. | Pre-fix `python3 scripts/validate_task_system.py` failed on the new guardrails as expected; post-fix `python3 scripts/validate_task_system.py`. | No runtime behavior exists yet; task 090 replaced the moving latest-stable policy with a fixed Zig `0.16.0` policy. | Historical blocker policy is superseded by the pinned Zig contract used by future dogfood and CI work. | `tasks/000-project-bootstrap.md`. |
| 088 AI Input Report Option Clarity | 2026-05-20 | Task metadata, task 000 guard, CLI spec, doctest AI docs, doctest mutation strategy, AI task requirements, and validator guardrails. | Structural validator guardrails requiring AI commands to use `--input-report <path>` and rejecting AI `--report <path>` wording. | Pre-fix `python3 scripts/validate_task_system.py` failed on the new guardrails as expected; post-fix `python3 scripts/validate_task_system.py`. | No runtime behavior exists yet; future AI commands now separate report input paths from run report formats. | Prevents AI and doctest dogfood scripts from confusing report input with report format selection. | `tasks/000-project-bootstrap.md`. |
| 087 Dogfood Release Gate Sequencing | 2026-05-20 | Task metadata, task 000 guard, task 058 follow-up, task 059 rename/scope, new task 085 final dogfood gate, task 060 dependency, F-025 gap row, and validator guardrails. | Structural validator guardrails requiring initial dogfood wording, final dogfood gate task 085, and task 060 dependency on task 085. | Pre-fix `python3 scripts/validate_task_system.py` failed on the new guardrails as expected; post-fix `python3 scripts/validate_task_system.py`. | No runtime behavior exists yet; release acceptance is now sequenced after final dogfood evidence. | Prevents initial advisory dogfood CI from being mistaken for the final release dogfood gate. | `tasks/000-project-bootstrap.md`. |
| 086 Validator Scope and Report Semantic Clarity | 2026-05-20 | Task metadata, task 000 guard, agent guide, status handoff, report-schema task wording, and validator guardrails. | Structural validator guardrails requiring validator-scope wording and task 006 semantic-validator derived-invariant coverage. | Pre-fix `python3 scripts/validate_task_system.py` failed on the new guardrails as expected; post-fix `python3 scripts/validate_task_system.py`. | No runtime behavior exists yet; agents now distinguish task-system validation from product semantic correctness. | Prevents future dogfood and report work from treating governance validation as behavioral proof. | `tasks/000-project-bootstrap.md`. |
| 084 Agent Contract Cutover Closure | 2026-05-20 | Task metadata, task lifecycle docs, property-test policy, pre-artifact handoff docs, task 000 guard, and validator guardrails. | Structural validator guardrails requiring pre-041 lifecycle cutovers, pre-044/pre-062 property-test staging, and `completion_evidence` subset wording. | Pre-fix `python3 scripts/validate_task_system.py` failed on the new guardrails as expected; post-fix `python3 scripts/validate_task_system.py`; `python3 -m py_compile scripts/validate_task_system.py`; JSON syntax checks; `git diff --check`. | No runtime behavior exists yet; pre-bootstrap lifecycle gates now distinguish available status evidence from later pipeline artifacts and mutation/property infrastructure. | Prevents agents from blocking project bootstrap on unavailable pipeline, mutation, or property-test gates while preserving those gates after their cutovers. | `tasks/000-project-bootstrap.md`. |
| 083 Agent Tooling Contract Hardening | 2026-05-20 | Task metadata, queue schema and explicit orders, follow-up task references, report/Zig contract docs, task 005/006 requirements, and validator guardrails. | Structural validator guardrails requiring explicit task orders, canonical follow-up refs, report semantic validation wording, and durable latest-stable Zig evidence wording. | Pre-fix `python3 scripts/validate_task_system.py` failed on the new guardrails as expected; post-fix `python3 scripts/validate_task_system.py`; `python3 -m py_compile scripts/validate_task_system.py`; JSON syntax checks for task-control files and schemas; `git diff --check`. | No runtime behavior exists yet; machine-readable task ordering and future report validation contracts are stricter. | Removes pre-bootstrap tooling ambiguity before dogfoodable behavior exists; no dogfood run expected. | `tasks/000-project-bootstrap.md`. |
| 082 Analysis Findings Closure | 2026-05-20 | Doctest fence docs, doctest parser task requirements, task gate headings, task 000 scope, Zig version policy wording, and task-system validator guardrails. | Structural validator guardrails for doctest fence-length contracts, canonical task gate headings, task 000 task-control file scope, and latest-stable Zig official-source policy. | Pre-fix `python3 scripts/validate_task_system.py` failed on the new guardrails as expected; post-fix `python3 scripts/validate_task_system.py`; `python3 -m py_compile scripts/validate_task_system.py`; JSON syntax checks for task-control files; stale-contract scan; `git diff --check`. | No runtime behavior exists yet; future agents now have unambiguous doctest fence rules and stronger task gate validation. | Removes pre-bootstrap ambiguity before dogfoodable behavior exists; no dogfood run expected. | `tasks/000-project-bootstrap.md`. |
| 081 Task-Local Validator Requirements | 2026-05-20 | Task metadata, early queued task required-test lists, task 000 guard, and validator guardrail. | Structural validator guardrail requiring non-superseded task files to list `python3 scripts/validate_task_system.py` in required tests. | Pre-fix `python3 scripts/validate_task_system.py` failed on tasks `000` through `024`, including `019`, as expected; post-fix `python3 scripts/validate_task_system.py`; `python3 -m py_compile scripts/validate_task_system.py`; `git diff --check`. | No runtime behavior exists yet; task-local completion gates now match the global validator standard. | Future agents see the validator command in each queued task instead of relying on global workflow memory. | `tasks/000-project-bootstrap.md`. |
| 080 Doctest Survivor Example Identity Guard | 2026-05-20 | Task metadata, doctest AI survivor example, task 000 guard, and validator guardrail. | Structural validator guardrail requiring `doctest_survivor` examples to keep `source_case.id` as `dt_...` and `mutation_case.id` as `dm_...`. | Pre-fix `python3 scripts/validate_task_system.py` failed on the stale `dt_...` mutation-case example as expected; post-fix `python3 scripts/validate_task_system.py`; `python3 -m py_compile scripts/validate_task_system.py`; `git diff --check`. | No runtime behavior exists yet; doctest survivor AI examples now preserve the documented ID split. | Future doctest mutation dogfood and survivor AI work can join ordinary cases, mutation entries, and survivor refs without copying an invalid example. | `tasks/000-project-bootstrap.md`. |
| 079 Status Completion Evidence Schema | 2026-05-20 | Task metadata, task 000 guard, status schema, structured status evidence, protocol/guide wording, and validator guardrails. | Structural validator guardrail requiring `completion_evidence` for every completed task plus schema guardrail requiring the field. | Pre-fix `python3 scripts/validate_task_system.py` failed on missing `completion_evidence` as expected; post-fix `python3 scripts/validate_task_system.py`; `python3 -m py_compile scripts/validate_task_system.py`; JSON syntax checks; `git diff --check`. | No runtime behavior exists yet; pre-artifact task completion evidence is now machine-checkable. | Fresh agents can verify completed-task evidence before dogfood and pipeline artifact tasks exist. | `tasks/000-project-bootstrap.md`. |
| 078 Bootstrap TDD Order | 2026-05-20 | Task metadata, task 000 guard and TDD wording, and validator guardrail. | Structural validator guardrail requiring task 000 to add bootstrap tests before `build.zig`. | Pre-fix `python3 scripts/validate_task_system.py` failed on task 000's stale bootstrap order as expected; post-fix `python3 scripts/validate_task_system.py`; `python3 -m py_compile scripts/validate_task_system.py`. | No runtime behavior exists yet; task 000 now preserves TDD-first ordering from the first scaffold change. | Keeps the first implementation task from bypassing failing evidence before dogfoodable behavior exists. | `tasks/000-project-bootstrap.md`. |
| 077 Backlog Audit Task Creation Scope | 2026-05-20 | Task metadata, task 000 guard, task 025 scope/allowed files, and validator guardrail. | Structural validator guardrail requiring task 025 to allow next-unused task markdown creation for concrete audit gaps. | Pre-fix `python3 scripts/validate_task_system.py` failed on task 025's missing task-file allowance as expected; post-fix `python3 scripts/validate_task_system.py`; `python3 -m py_compile scripts/validate_task_system.py`. | No runtime behavior exists yet; future backlog audit agents can create concrete missing tasks without violating allowed-file scope. | Prevents the audit from becoming a human-coordination blocker before dogfood expansion tasks. | `tasks/000-project-bootstrap.md`. |
| 076 Doctest Mutation Entry Identity | 2026-05-20 | Task metadata, task 000 guard, doctest identity docs, task 061/067 requirements, and validator guardrail. | Structural validator guardrail requiring `dm_...` mutation-aware doctest entry identity wording. | Pre-fix `python3 scripts/validate_task_system.py` failed on missing `dm_...` identity wording as expected; post-fix `python3 scripts/validate_task_system.py`; `python3 -m py_compile scripts/validate_task_system.py`. | No runtime behavior exists yet; future mutation-aware doctest reports now have unambiguous entry IDs. | Future doctest mutation dogfood and survivor AI can join source cases, mutation entries, and survivor refs without ambiguity. | `tasks/000-project-bootstrap.md`. |
| 075 Mutation Gate Availability Policy | 2026-05-20 | Task metadata, task 000 guard, mutation gate policy, task 043 wording, and validator guardrail. | Structural validator guardrail requiring task 043 cutover and `pre-gate unavailable` policy wording. | Pre-fix `python3 scripts/validate_task_system.py` failed on missing mutation-gate availability wording as expected; post-fix `python3 scripts/validate_task_system.py`; `python3 -m py_compile scripts/validate_task_system.py`. | No runtime behavior exists yet; mutation-gate requirements now have an explicit availability cutover. | Preserves mandatory future mutation gates while preventing pre-gate tasks from blocking on nonexistent tooling; no dogfood run expected yet. | `tasks/000-project-bootstrap.md`. |
| 074 Same-File Exclusion Sequencing | 2026-05-20 | Task metadata, task 000 guard, task 009/010/018/019/020 sequencing metadata, and validator guardrail. | Structural validator guardrail rejecting task 019 after task 010 and requiring task 020 to depend on tasks 018 and 019. | Pre-fix `python3 scripts/validate_task_system.py` failed on same-file exclusion ordering as expected; post-fix `python3 scripts/validate_task_system.py`; `python3 -m py_compile scripts/validate_task_system.py`. | No runtime behavior exists yet; I-009 implementation is now sequenced before first stable candidate-emitting mutator. | Future dogfood candidate sets are protected from normal test-body mutation by task order; no dogfood run expected yet. | `tasks/000-project-bootstrap.md`. |
| 073 Agent Protocol Readiness Order | 2026-05-20 | Task metadata, task 000 guard, startup-order docs, and task-system validator guardrail. | Structural validator guardrail rejecting mark-active-before-read startup wording. | Pre-fix `python3 scripts/validate_task_system.py` failed on stale startup-order wording as expected; post-fix `python3 scripts/validate_task_system.py`; `python3 -m py_compile scripts/validate_task_system.py`. | No runtime behavior exists yet; autonomous task startup ordering is stricter and validator-backed. | Removes pre-bootstrap startup-order ambiguity before dogfoodable behavior exists; no dogfood run expected. | `tasks/000-project-bootstrap.md`. |
| 072 Prebootstrap Sequencing and Contract Cleanup | 2026-05-20 | Task metadata, task 064 dependency and guard, task 000 guard, validator guardrail, harness and roadmap wording, and invariant gap row. | Structural validator guardrail for direct previous non-superseded execution-order dependencies. | Pre-fix `python3 scripts/validate_task_system.py` failed on task `064` missing dependency `062` as expected; post-fix `python3 scripts/validate_task_system.py`; `python3 -m py_compile scripts/validate_task_system.py`; JSON syntax checks for task state and invariant gap registry; `git diff --check`. | No runtime behavior exists yet; queue execution ordering is stricter and validator-backed. | Removes pre-bootstrap sequencing risk before dogfoodable behavior exists; no dogfood run expected. | `tasks/000-project-bootstrap.md`. |
| 071 Agent Contract Finalization | 2026-05-20 | Task metadata, Codex orchestrator routing, pipeline schema ownership contracts, schema gap registry rows, task-system validator guardrails, root `.gitignore`, and stale status history. | Structural validator guardrails for escalation-policy routing sync, task 063 baseline pipeline schema ownership, Zig build artifact ignore rules, and stale untracked-baseline status wording. | Pre-fix `python3 scripts/validate_task_system.py` failed on the new guardrails as expected; post-fix `python3 scripts/validate_task_system.py`; `python3 -m py_compile scripts/validate_task_system.py`; JSON syntax checks for task state and schema gap registry; `git diff --check`. | No runtime behavior exists yet; future autonomous agent routing and pipeline schema ownership are stricter and validator-backed. | Removes remaining pre-bootstrap agent-contract drift before dogfoodable behavior exists; no dogfood run expected. | `tasks/000-project-bootstrap.md`. |
| 070 Agent Blocker Contract Closure | 2026-05-20 | Task metadata, validator guardrails, bootstrap/test-harness task contracts, pipeline artifact docs, schema registry rows, doctest docs, README, and status prose. | Structural validator guardrails for follow-up execution order, bootstrap test discovery ownership, active-lock artifacts, doctest expectation kinds, stale status prose, and README orientation. | Pre-fix `python3 scripts/validate_task_system.py` failed on the new guardrails as expected; post-fix `python3 scripts/validate_task_system.py`; `python3 -m py_compile scripts/validate_task_system.py`; JSON syntax checks for task state and gap registry; `git diff --check`. | No runtime behavior exists yet; future deterministic implementation contracts are stricter and validator-backed. | Removes agent-blocking contract gaps before project bootstrap; no dogfood run expected. | None predefined. |
| 069 Agent Readiness Contract Closure | 2026-05-20 | Task metadata, CLI/config/report/doctest contracts, mutation agent role profile, and task-system validator guardrails. | Structural validator guardrails for run-option ownership, config worker-count coverage, mutation-agent `compiler_crash`, baseline timeout semantics, pipeline validator ordering, and doctest snapshot modes. | Pre-fix `python3 scripts/validate_task_system.py` failed on the new guardrails as expected; post-fix `python3 scripts/validate_task_system.py`; `python3 -m py_compile scripts/validate_task_system.py`; JSON syntax checks for task state; Markdown JSON fence parse; `git diff --check`. | No runtime behavior exists yet; future deterministic implementation contracts are now stricter and validator-backed. | Removes ambiguity before dogfoodable behavior exists; no dogfood run expected. | None predefined. |
| 068 Preimplementation Contract Alignment | 2026-05-20 | Task metadata, contract docs, report schema, and matching gap registry rows. | None; docs/schema contract alignment used existing validator and syntax checks. | `python3 scripts/validate_task_system.py`; `python3 -m py_compile scripts/validate_task_system.py`; JSON syntax checks for edited JSON files; Markdown JSON fence parse; `git diff --check`. | No runtime behavior exists yet; report and doctest contracts clarified for future deterministic implementations. | Removes ambiguity before dogfoodable behavior exists; no dogfood run expected. | None predefined. |

## Consistency Record

| Date | Scope | Verification |
| --- | --- | --- |
| 2026-05-19 | Resolved documentation, schema registry, follow-up task, and backlog coverage inconsistencies found during read-only consistency review. | `python3 scripts/validate_task_system.py` passed for the then-current task set. |
| 2026-05-19 | Added Kumo-inspired governance docs adapted to zentinel: glossary, non-goals, invariants, harness, discipline, style, failure modes, ADR system, and docs-to-tests gap registries. | `python3 scripts/validate_task_system.py` passed with governance, ADR, and gap-registry checks. |
| 2026-05-20 | Added Codex-only `.agents/` operating layer with orchestrator contract, role profiles, and workflow runbooks; explicitly kept Claude-specific `.claude/` files out of zentinel. | `python3 scripts/validate_task_system.py` passed for the then-current task set; no `.claude/` directory exists. |
| 2026-05-20 | Refined task, schema, CI, runner, and agent contracts to remove speculation points before implementation starts. | `python3 scripts/validate_task_system.py` passed for the then-current task set; JSON syntax checks passed for task state, AI context schema, and mutator gap registry. |
| 2026-05-20 | Refined remaining agent-readiness gaps: shared command parser ownership, complete AI context examples, CLI Phase 0 ownership, init option task ownership, nested test-discovery fixture scope, JUnit mapping, backend/operator stability fields, and task 025 filename alignment. | `python3 scripts/validate_task_system.py` passed for the then-current task set; JSON syntax checks passed for task state, report schema, AI context schema, and schema gap registry. |
| 2026-05-20 | Reduced implementation speculation around init option test ownership, command parser grammar/error code, report baseline semantics, structured command evidence, strict test-selection metadata, AI prompt/doctest response schema registry entries, and roadmap phase semantics. | `python3 scripts/validate_task_system.py` passed for the then-current task set; JSON syntax checks passed for task state, queue, report schema, AI context schema, and schema gap registry. |
| 2026-05-20 | Resolved another deep-analysis pass: task-scoped pipeline artifact exception, canonical JSON handoffs, doctest report schema ownership, executable doctest examples, report/AI command evidence strictness, baseline policy, cache diagnostics, AI prompt context shape, CLI option ownership, prose-only follow-up enforcement, schema ownership validation, and concrete follow-up task metadata later extended through task 067. | `python3 scripts/validate_task_system.py` passed for the then-current task set; Python validator compilation and JSON syntax checks passed. |
| 2026-05-20 | Applied follow-up fixes: explicit execution-order keys for inserted prerequisite tasks, release acceptance moved to the final non-superseded order slot, report and AI context command-result evidence aligned, doctest error-code ownership documented, Zig version examples made version-agnostic, and JSON verification artifacts added to pipeline contracts. | `python3 scripts/validate_task_system.py` passed for the then-current task set; Python validator compilation and JSON syntax checks passed. |
| 2026-05-20 | Applied product-choice and consistency fixes: doctest AI is a user-facing CLI surface, doctest mutation uses `--format`, AI command report/provider/ref semantics are explicit, report run-level invariants are schema-enforced, lifecycle queue states are separated from pipeline artifact stages, agent wording uses dependency-ready execution order, mutator tables are escaped, and pipeline schema validation scope is narrowed to a standard-library subset. | `python3 scripts/validate_task_system.py` passed for the then-current task set; stale-phrase scan found no matches; JSON syntax checks passed for report schema and task state. |
| 2026-05-20 | Applied another contract refinement pass: doctest case references are explicit, `doctest suggest` no longer requires a report, doctest explain response schema ownership is defined, AI error codes are specific, config exclude defaults are exact, report-local display IDs are clarified, test-selection command ordering preserves config order, doctest AI CLI commands are in roadmap and acceptance criteria, and task 006/016 metadata is aligned with report evidence and snapshots. | `python3 scripts/validate_task_system.py` passed for the then-current task set; Python validator compilation, JSON syntax checks, JSON fence checks, stale-phrase scans, and `git diff --check` passed. |
| 2026-05-20 | Applied follow-up contract fixes: duplicate unlabeled identical doctest cases are invalid, doctest source refs use anchor lines plus secondary `block_refs`, shared AI explain schema includes doctest labels, AI/doctest error codes are covered by failure modes and gap rows, remote AI config policy is explicit, AI redaction defaults are exact, validator guardrails cover these contracts, and the repository scaffold baseline was prepared for tracking. | `python3 scripts/validate_task_system.py` passed for the then-current task set; Python validator compilation and `git diff --check` passed. |
| 2026-05-20 | Applied doctest AI/report contract refinements: prompt requests now have registered context-schema ownership for mutation and doctest flows, `doctest review-snapshot` is a user-facing CLI subcommand, doctest AI context/suggestion/snapshot schemas have exact targets, doctest report v1 has concrete report/failure fields, doctest AI output persistence is advisory-only, and public-doc doctest coverage includes `docs/DOCTEST_AI_INTEGRATION.md`. | `python3 scripts/validate_task_system.py` passed for the then-current task set; JSON syntax checks and `git diff --check` passed. |
| 2026-05-20 | Hardened runtime and doctest AI contracts from the latest analysis: `compiler_crash` is a distinct deterministic status, Zig cache reuse and concurrent workspaces are isolated, allocator mutators are bounded to injected target wrappers, doctest case kinds and snapshot evidence are exact, gap-registry row updates have a narrow exception, and task 067 covers `zentinel doctest explain-survivor`. | `python3 scripts/validate_task_system.py` passed for the then-current task set; Python validator compilation, JSON syntax checks, and `git diff --check` passed. |
| 2026-05-20 | Resolved the latest external-agent and deep-analysis findings: task 061 can edit `docs/DOCTEST_SPEC.md`, mutation-aware doctest reports have exact `summary.mutation`, `case.mutation`, and `ds_...` survivor-ref contracts, task 055/067 doctest AI schema ownership is split, doctest AI evidence objects are closed, and F-033 no longer points to the administrative backlog audit task. | `python3 scripts/validate_task_system.py` passed for the then-current task set; Python validator compilation, JSON syntax checks, JSON fence checks, and `git diff --check` passed. |
| 2026-05-20 | Completed preimplementation contract alignment task 068: task 025 now covers inserted tasks 061-067, Roadmap Phase 2 treats preview mutators as backlog, doctest mutation summaries and survivor AI context shapes are exact, pre-041 pipeline artifact wording is aligned, and report `internal_error` has deterministic `run.error` schema ownership. | `python3 scripts/validate_task_system.py` passed for the then-current task set; Python validator compilation, edited JSON syntax checks, Markdown JSON fence checks, changed-file scope check, and `git diff --check` passed. |
| 2026-05-20 | Completed agent-readiness contract closure task 069: run-option ownership is explicit and allowed-file backed, config worker-count and validation coverage is owned, mutation-agent `compiler_crash` wording is aligned, baseline timeout report semantics are defined, pipeline metadata validation now executes immediately after task 041, and doctest snapshot modes are synchronized. | `python3 scripts/validate_task_system.py` passed for the then-current task set; Python validator compilation, JSON syntax checks, Markdown JSON fence checks, stale-contract scan, and `git diff --check` passed. |
| 2026-05-20 | Completed prebootstrap sequencing and contract cleanup task 072: task 064 now follows task 062 by direct dependency, the validator enforces direct previous-task dependencies, roadmap scaffold wording avoids unowned directories, handoff artifacts are canonical JSON, harness mode evidence wording matches report placement, and invariant gap wording is current. | `python3 scripts/validate_task_system.py` passed; Python validator compilation, JSON syntax checks, and `git diff --check` passed. |

## Blockers

No known blockers.

## Handoff Notes

No task is active. The next agent should run `python3 scripts/validate_task_system.py`, start with `tasks/000-project-bootstrap.md`, create the minimal Zig project scaffold, and follow `docs/TDD_POLICY.md` from the first behavior-bearing change. The validator checks task-system consistency, not product semantic correctness; product behavior still requires the active task's failing tests and verification gates.

Governance docs are available under `docs/GLOSSARY.md`, `docs/NON_GOALS.md`, `docs/INVARIANTS.md`, `docs/HARNESS.md`, `docs/DISCIPLINE.md`, `docs/STYLE.md`, `docs/FAILURE_MODES.md`, `docs/GAP_REGISTRIES.md`, and `docs/adr/README.md`.

Pipeline architecture specs are available under `docs/AGENT_PIPELINE_ARCHITECTURE.md`, `docs/AGENT_ROLE_SPEC.md`, `docs/HANDOFF_CONTRACTS.md`, `docs/AGENT_CONTEXT_PACKETS.md`, `docs/VERIFICATION_PIPELINE.md`, and related policy files. Pipeline hardening tasks `040` through `049` are queued after the doctest task block, with additional inserted prerequisite and follow-up tasks `061` through `070`, final dogfood gate task `085`, plus pre-bootstrap hardening tasks `071` through `105`, placed by execution order before project bootstrap. The historical hardening range through task `099` was completed before task `100` clean handoff lifecycle closure.

Codex-specific operating profiles are available under `.agents/`. Use `.agents/README.md`, `.agents/ORCHESTRATOR.md`, `.agents/roles/`, and `.agents/workflows/` for role dispatch and workflow execution. Do not add `.claude/` to this repository.

The repository scaffold has been prepared as the baseline project state. Future agents should still use the task queue, status files, and explicit handoff notes as the durable source of truth, but `git status` may be used to detect changes made after the baseline is tracked.

Task `100` completed at execution order `000.0.30` before project bootstrap. Task `101` completed at execution order `000.0.31` before project bootstrap. Task `102` completed at execution order `000.0.32` before project bootstrap. Task `103` completed at execution order `000.0.33` before project bootstrap. Task `104` completed at execution order `000.0.34` before project bootstrap. Task `105` completed at execution order `000.0.35` before project bootstrap. Future agents should start `tasks/000-project-bootstrap.md` next after respecting the clean handoff boundary.
